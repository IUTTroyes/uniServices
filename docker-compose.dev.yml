services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    environment:
      APP_ENV: dev
      APP_DEBUG: "1"
      MAILER_DSN: "smtp://maildev:1025"
    volumes:
      - ./back:/var/www/html/back
      - ./packages:/var/www/html/packages
      - ./shared:/var/www/html/shared
    depends_on:
      db:
        condition: service_healthy

  nginx:
    ports:
      - "${WEB_PORT:-8080}:80"
    volumes:
      - ./docker/nginx/dev.conf:/etc/nginx/conf.d/default.conf:ro
      - ./back:/var/www/html/back
    depends_on:
      - php
      - front-auth
      - front-intranet
      - front-edt
      - front-unifolio

  db:
    ports:
      - "${DB_PORT:-3306}:3306"

  phpmyadmin:
    image: arm64v8/phpmyadmin
    container_name: uniservices-phpmyadmin
    restart: unless-stopped
    ports:
      - "${PHPMYADMIN_PORT:-8081}:80"
    environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - uniservices_net

  maildev:
    image: maildev/maildev
    container_name: uniservices-maildev
    command: bin/maildev --web 1080 --smtp 1025 --hide-extensions STARTTLS
    restart: unless-stopped
    ports:
      - "${MAILDEV_PORT:-8082}:1080"
    networks:
      - uniservices_net

  installer:
    image: node:22-alpine
    container_name: uniservices-installer
    working_dir: /app
    command: sh -c "corepack enable && corepack prepare pnpm@latest --activate && pnpm install"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    networks:
      - uniservices_net

  front-auth:
    image: node:22-alpine
    container_name: uniservices-front-auth
    working_dir: /app
    command: sh -c "corepack enable && pnpm run dev:auth --host --no-open"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      VITE_API_URL: http://nginx:80
      VITE_HMR_CLIENT_PORT: ${WEB_PORT:-8080}
    networks:
      - uniservices_net
    depends_on:
      installer:
        condition: service_completed_successfully

  front-intranet:
    image: node:22-alpine
    container_name: uniservices-front-intranet
    working_dir: /app
    command: sh -c "corepack enable && pnpm run dev:intranet --host"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      VITE_API_URL: http://nginx:80
      VITE_HMR_CLIENT_PORT: ${WEB_PORT:-8080}
    networks:
      - uniservices_net
    depends_on:
      installer:
        condition: service_completed_successfully

  front-edt:
    image: node:22-alpine
    container_name: uniservices-front-edt
    working_dir: /app
    command: sh -c "corepack enable && pnpm run dev:edt --host"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      VITE_API_URL: http://nginx:80
      VITE_HMR_CLIENT_PORT: ${WEB_PORT:-8080}
    networks:
      - uniservices_net
    depends_on:
      installer:
        condition: service_completed_successfully

  front-unifolio:
    image: node:22-alpine
    container_name: uniservices-front-unifolio
    working_dir: /app
    command: sh -c "corepack enable && pnpm run dev:unifolio --host"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      VITE_API_URL: http://nginx:80
      VITE_HMR_CLIENT_PORT: ${WEB_PORT:-8080}
    networks:
      - uniservices_net
    depends_on:
      installer:
        condition: service_completed_successfully

volumes:
  node_modules:

networks:
  uniservices_net:
