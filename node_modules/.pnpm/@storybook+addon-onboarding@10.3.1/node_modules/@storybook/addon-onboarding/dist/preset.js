import CJS_COMPAT_NODE_URL_00mkxftgf89y5 from 'node:url';
import CJS_COMPAT_NODE_PATH_00mkxftgf89y5 from 'node:path';
import CJS_COMPAT_NODE_MODULE_00mkxftgf89y5 from "node:module";

var __filename = CJS_COMPAT_NODE_URL_00mkxftgf89y5.fileURLToPath(import.meta.url);
var __dirname = CJS_COMPAT_NODE_PATH_00mkxftgf89y5.dirname(__filename);
var require = CJS_COMPAT_NODE_MODULE_00mkxftgf89y5.createRequire(import.meta.url);

// ------------------------------------------------------------
// end of CJS compatibility banner, injected by Storybook's esbuild configuration
// ------------------------------------------------------------

// src/preset.ts
import { telemetry } from "storybook/internal/telemetry";

// package.json
var version = "10.3.1";

// src/constants.ts
var ADDON_ID = "storybook/onboarding", ADDON_ONBOARDING_CHANNEL = `${ADDON_ID}/channel`;

// src/preset.ts
var experimental_serverChannel = async (channel, options) => {
  let { disableTelemetry } = await options.presets.apply("core", {});
  return disableTelemetry || channel.on(ADDON_ONBOARDING_CHANNEL, ({ type, ...event }) => {
    type === "telemetry" ? telemetry("addon-onboarding", { ...event, addonVersion: version }) : type === "survey" && telemetry("onboarding-survey", { ...event, addonVersion: version });
  }), channel;
};
export {
  experimental_serverChannel
};
